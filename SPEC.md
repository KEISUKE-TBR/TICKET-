# Card Layout Studio MVP 仕様書

## 1. 目的

CSVに登録された「贈り主」「メッセージ」「QRコード」を、編集可能なカードテンプレートへ差し込み、エレコム MT-HMN2WNR用のA4・8面レイアウトとして印刷またはPDF保存する。

## 2. 想定利用者

- イベント、ギフト、メッセージカードを少量〜中量発行する担当者
- Illustrator等で毎回差し込み作業をしたくない利用者
- 個別の長文だけ文字サイズを調整したい利用者

## 3. MVPの機能範囲

### 3.1 CSVデータ

- CSV・TSV読み込み
- UTF-8 / Shift-JIS / 自動判定
- 見出し行から列を選択
- 管理ID、贈り主、メッセージ、QRの4項目へマッピング
- 印刷対象のON/OFF
- 読込件数、印刷対象件数、ページ数の表示

### 3.2 テンプレート編集

- カードサイズ：86 × 54mm
- プレースホルダー追加
  - 贈り主
  - メッセージ
  - QRコード
  - 管理ID
- 固定テキスト追加
- 固定画像追加
- ドラッグ移動
- 右下ハンドルによるリサイズ
- X / Y / 幅 / 高さの数値入力
- 文字サイズ、最小文字サイズ、行間、太さ、横揃え、縦揃え
- テキスト自動縮小
- 要素の複製、削除、前面・背面移動
- 背景色
- 任意CSV行でのプレビュー
- テンプレートJSONの入出力
- ローカル自動保存

### 3.3 面付け・印刷

- A4縦・2列×4段
- 8件ごとの自動改ページ
- 9件目を2ページ目へ配置
- テスト枠ON/OFF
- 全体の横・縦補正
- 印刷プレビュー
- ブラウザ印刷
- Chrome印刷画面からPDF保存

### 3.4 個別調整

- プレビュー上のカード選択
- 要素単位の個別調整
- 内容上書き
- X / Y / 幅 / 高さ
- 文字サイズ / 最小文字サイズ
- 個別調整の解除
- テンプレート本体を変更せず、対象カードだけへ適用

## 4. 自動縮小ルール

1. テンプレート指定の文字サイズで描画
2. `scrollHeight > clientHeight` または `scrollWidth > clientWidth` を判定
3. 0.25ptずつ縮小
4. 最小文字サイズまで縮小
5. それでも収まらない場合、印刷プレビューに警告「!」を表示
6. 警告は印刷時には表示しない

## 5. QR画像ルール

入力値を以下の順序で判定する。

1. `data:image/`：そのまま使用
2. `blob:`：そのまま使用
3. `=IMAGE("URL")`：URLを抽出
4. `http://` / `https://`：画像URLとして使用
5. その他：QuickChartのQR生成URLへ変換

## 6. 用紙仕様

- A4：210 × 297mm
- カード：86 × 54mm
- X座標：14mm / 110mm
- Y座標：25.5 / 89.5 / 153.5 / 217.5mm
- 列間：10mm
- 段間：10mm

## 7. 非機能要件

- サーバー不要
- ブラウザ内処理
- CSVデータを永続保存しない
- テンプレート・個別調整・補正値のみlocalStorage保存
- Chrome最新版を主対象
- A4印刷時にCSS mm単位で配置
- 外部ライブラリ不使用
- オフライン起動可能
  - 外部画像URLの表示時のみインターネット接続が必要

## 8. MVP対象外

- GoogleスプレッドシートOAuth直接連携
- 複数ユーザー・アカウント
- クラウド保存
- PDFバイナリの直接ダウンロード
- 裏面デザイン、両面印刷の自動面順変換
- 任意用紙サイズのプリセット編集
- QR画像ZIP一括照合
- 文字の縦書き
- Illustrator/PDFテンプレート取込

## 9. 受け入れ条件

- サンプルCSVを読み込める
- 「贈り主」「メッセージ」「QRコード」がカードへ反映される
- 8件がA4 1ページに配置される
- 9件以上で2ページ目が生成される
- 特定カードのメッセージだけ文字サイズを変更できる
- テスト枠を非表示にできる
- 印刷画面でA4・100%設定により出力できる
- リロード後もテンプレートが残る
